# Review of 08f53f7..ae21c3f (2026-09-23) — Needs fixes (0 Critical, 1 Important)
All 4 items meet the brief; no path credits sleep without a live-observed wake.
IMPORTANT: wake grace collapses to ~100 ms for timebox/Pomodoro sessions — deferred heartbeat doesn't advance sampled_ms, so ms_until_boundary = remaining − pending(sleep) = 0 → loop waits only BOUNDARY_MARGIN (50 ms); ticks 2–3 fire ~100 ms later and the gap rule pauses with no credit if a tick lands between wake and NSWorkspaceDidWakeNotification (focus_window.rs ~236-249; engine.rs ms_until_boundary ~583/612; heartbeat deferral ~739-747). Fix: time-based grace (store first-deferral clock sample in SleepMark, fall back after ~60 s) or ms_until_boundary → None while a sleep mark has a pending gap; add loop-level/pure test of the deferred delay.
Minors: (1) removal Undo can outlive 10 s (sonner pauses on hover/hidden) — record issuedAt, refuse after FOCUS_UNDO_MS; (2) companion bottom-center toast may cover timer/Pause in compact — consider top-center; (3) cache chrome only if in plausible band 0–80; (4) clampFocusPosition adds chrome in expanded mode (pre-existing); (5) wall-clock stamps for boundaries inside sleep off (ms correct); (6) no render test of tray toast wiring. Product calls for Marco: chime on wake after ≤30 min sleep crossing a boundary; "Inbox" label on local-only tasks.
