# Proposed /admin adapter — not activated

After an approved source switch, use `dt --json task list` and exact `task get ID` reads to assemble the ranked docket. Preserve the existing admin skill's approval boundaries for external actions. CLI access itself grants no email, calendar, financial or Todoist authorization.

Draft outcomes first. Once the relevant task changes are authorized, update existing IDs, merge descriptions and preserve the complete label set. Complete a task only after its real-world outcome is verified. Record a Nimble product limitation with `dt --json gap "reason"` when requested by the trial workflow; gap recording is not a replacement task.

Select Nimble or the authorized Todoist fallback before writing. A successful mutation with `refresh: unavailable`, a partial batch, or a lost acknowledgement requires reading back current state. Never automatically restart the batch or write the same outcome to both systems. Keep Instinct's existing ownership until Marco explicitly changes it.
