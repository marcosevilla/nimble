# Focus Queue absorption — every ruling made on Marco's behalf

Extracted from the SDD ledger on 2026-09-23 (in order). Cost-if-wrong is in each line.

- 34:Ruling: Native worktree tool cannot target the nested Git root; use git worktree on current Nimble HEAD — native call returned Not a git repository — cost if wrong: checkout would need reattachment, source remains intact.
- 95:Ruling: Task 5 scope includes the handoffs Task 3/4 reports assign to it (route desktop task commands, app-handled dt RPC, and incoming Todoist/Turso/Calendar local applies through the single FocusService via begin_task_write; web provider reads readFocusReplica) even where the plan's Task 5 file list omits those files — the verification record and NEXT name them as Task 5 gates — cost if wrong: larger Task 5 diff that Task 8/9 would otherwise have touched.
- 98:Ruling: backup_recovery::both_routes_round_trip_without_changing_source fails since Task4 round1 (0a6117e) — export route asserts settings empty, but the intended restored_activation_required=1 marker now lives there; Task4 review only ran focused tests. Fix the test expectation (settings holds exactly that marker) in the Task5 fix loop, not the marker — cost if wrong: one test line.
- 101:Ruling: owner-only sound token deferred to Task 9 (no sound emitter exists yet; Task 9 owns companion/owner lifecycle and review focus pins sound in 5 and 9) — cost if wrong: Task 9 adds a field to the event payload.
- 102:Ruling: remote recurring due change reconciled as plain edit without pause stands — spec says "a remote due change alone is treated as an edit" — cost if wrong: a live session keeps running on an advanced occurrence until user acts.
- 103:Ruling: web history ordering parity with FocusService::history — implementer verifies in fix round 1 (cheap check) — cost if wrong: history rows ordered differently on web.
- 128:Ruling: local-only source = `sync_policy === 'local_only'` (spec §52/§207 "explicitly unbound"), not merely external_id NULL — a NULL external_id default-policy task is pending Todoist export, not local — cost if wrong: unbound-but-default tasks missing from Local view; one predicate change.
- 129:Ruling: store refuses start/resume/start_break while capabilities.live_timing=false (plan: gate UI on capabilities; avoids 40s gap-pause trap) — cost if wrong: nothing startable until Task 9, which is already the case in practice.
- 132:Ruling: prompt header "Help me with this task from Nimble Focus" stands (does not trigger task-assist skill, which writes Todoist and would be wrong for local-only tasks; spec §76 makes routing changes explicit, not implicit) — flag to Marco at pause — cost if wrong: pasted prompts stop auto-routing to task-assist until the skill learns the new header.
- 133:Ruling: "omit already queued" by task_id stands — a task with any live queue entry must not be queued twice; Task 3 engine ends/replaces completed recurring occurrences — cost if wrong: an advanced recurring occurrence can't be re-queued until the stale entry is removed.
- 149:Ruling: keep "Nimble Focus" prompt header; updated ~/.claude/skills/task-assist/SKILL.md to recognize both headers and route write-back by the prompt's route lines (linked → Todoist MCP on external ID, description/subtasks; local-only → dt on Nimble ID, never Todoist; description checklist fallback until child inheritance is guaranteed) — cost if wrong: skill text edit.
- 150:Ruling: found latent gap — create_task_tx gives children of local_only parents sync_policy 'default' → Todoist create with unmapped parent (violates global constraint). Task 6b follow-up: child under local_only parent is forced local_only; also prompt route lines made actionable and External ID hidden for local_only (clears Task 6 deferred minor) — cost if wrong: a user wanting a synced child under a local parent must re-parent.
- 160:Ruling: S1 — surface "Copy assistant context" as a visible secondary icon control on the card (spec §4 lists it; Marco asked for the most optimal task-completion assist) rather than record a deviation — cost if wrong: one extra icon on the card.
- 161:Ruling: focus `complete` without expected due is safe — occurrence_id binds the recurrence generation (Task 3 expected-generation guard) — cost if wrong: recurring task advanced twice; whole-branch review to confirm.
- 179:Ruling: empty-queue entry point — the tray must be reachable with an empty queue (permanent entry via existing nav/shortcut) so inline Add/source browsing work from zero; include in Task 8 fix round — cost if wrong: one extra entry affordance.
- 198:Ruling: running timer display must interpolate between 20s heartbeats (display-only, from snapshot checkpoint + elapsed, never persisted/sent, snaps to authoritative snapshot, never runs backward) — a timer that jumps 20s at a time fails the "dominant timer" UX; not a clock authority since nothing is written — cost if wrong: small render hook to remove.
- 199:Ruling: minimize and ⌘H do NOT pause; only closing/hiding the last focus surface pauses (spec "last visible surface close pauses"; hiding the window while working is normal and must not silently stop tracking) — cost if wrong: time keeps accruing while minimized; user can pause explicitly.
- 205:Ruling: M1 companion inheriting whole default capability violates brief "exact required Tauri permissions" → treat as spec gap, fix in round 1 with a dedicated focus capability — cost if wrong: companion missing a permission, caught by native checklist.
- 206:Ruling: Pomodoro boundary gets a sound token (spec "signal once") — in scope for I2.
- 232:Ruling: fix at source — task write commands emit nimble-data-changed {version:1, domains:["tasks"], ids} after commit; companion also re-reads tasks when shown. Copy: menu header "Add tasks from"; trigger "From: <source>"; "Add N to queue" / disabled "All added"; "Local only" → "Nimble only" with "Not synced to Todoist" hint — cost if wrong: copy strings.
- 233:NATIVE-3 (Marco): no visible focus-queue entry in task detail or Inbox rows. Confirmed: only invisible `f` row hotkey (useTaskRowActions.ts:43) and BulkActionBar (multi-select only). Ruling: add visible entries — row menu/hover action "Add to focus queue" + "Focus now" (queued state shows "In focus queue" with remove), and a Focus control in TaskDetailPage header (Add to queue / Focus now) — queued as native fix 2 after fix 1 (sequential implementers) — cost if wrong: extra affordances.
- 243:Ruling: live/selected entry's row icon becomes a non-removing indicator (opens queue); remove only via menu, and Remove toast gets Undo (re-enqueue) — cost if wrong: one extra step to remove the live task.
- 254:Ruling: Up next gets roving row focus — ↑/↓ moves a visible focus ring between rows, Alt+↑/↓ reorders the focused row, Enter or an explicit "Move to top" action promotes; single click focuses/selects the row rather than promoting (Marco's feedback outranks the §4 click-to-promote mapping; promotion stays one explicit action away) — cost if wrong: one extra step to promote by mouse.
- 262:Task 10: BASE 367d600; implementer dispatched (Opus) 2026-09-22 with brief task-10-brief.md, report task-10-report.md. Rulings in dispatch: Ruling: preview token = deterministic hash of file hashes + destination revisions + selections; stale → reject/regenerate — spec §10 commit-only-against-unchanged-preview — cost if wrong: token format rework. Ruling: no new file-picker dependency; use existing Tauri dialog plugin if present, else report concern — dependency additions need Marco approval — cost if wrong: one follow-up UI step.
- 277:Ruling: legacy close intents cannot be adopted for remote send; "adopt" for a close means completing the task natively in Nimble (normal outbox close), the focus dispatcher only ever sends comments — Native tasks remain authoritative and a second close path risks double/stale closes — cost if wrong: one extra native click per undelivered legacy close.
- 278:Ruling: time comment fires on any LOCAL native completion of a mapped task's focused occurrence (focus Complete, task list, detail, dt), not on remote-pull completions — spec §9 ties the summary to occurrence completion, and remote completions would echo — cost if wrong: missing comments when a task is completed on another device.
- 293:Task 12: BASE 95bbb53. Ruling: the plan's step-5 "fresh whole-branch review" IS the SDD final review — controller runs it after Task 12, implementer does not — avoids a duplicate review seat — cost if wrong: none (same review).
- 294:Ruling: Task 12 split — implementer does suites, automatable synthetic native checks (controller-style osascript/DB reads in the frozen test worktree, reusing nimble-focus-test-target to save disk; 32 GB free), mock-harness state renders, backup/import round-trip, verification doc + a short human-only checklist; Marco runs only the human-only items afterwards — sleep/wake, real two-window racing and visual judgement need a person — cost if wrong: one more short native session.
- 302:Ruling: overturn Task 7 ruling (line 161: occurrence_id binds recurrence generation) — it does not; Complete must verify expected due identity — reviewer showed the concrete EDD path — cost if wrong: an extra stale_occurrence prompt.
- 303:Ruling: I4 fix = minimal explicit activation (settings/dt command that clears the restore marker and re-initializes ownership, with a test) + runbook line in NEXT; no broader recovery UI — disaster recovery must not dead-end — cost if wrong: small command surface to maintain.
- 304:Ruling: final fix batch = C1, I2, I3, I4 + triaged-before-merge minor (Task 10 duplicate completion keys double-count preview/included_ms) + Undo-delete task_labels sync_log (label loss on sync). Other minors stay deferred per reviewer triage.
- 307:parked — web focus replica stuck after restore activation — Ruling: park, not fixed in this branch; recorded in NEXT as a known gap for the disaster-recovery path — read-only web view only, no local data loss, only reachable after a real restore — cost if wrong: web focus view shows stale state after a restore until an epoch-replacing upsert is added.
- 2026-09-23 (Marco): a credited sleep (≤ 30 min) that crosses a timebox/Pomodoro boundary chimes on wake — kept as built, locked by `an_uncapped_sleep_that_crosses_a_timebox_chimes_on_wake`. Capped sleeps stay silent.
- 2026-09-23 (Marco): no "Inbox" label on local-only tasks on the focus card — skip.
- 2026-09-23: review Important (wake grace ~100 ms under boundary wakes) fixed in `35f2479` — time-based 60 s grace + no boundary wake while a sleep gap is pending.
