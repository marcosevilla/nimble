# Settings + setup — Loop 1 audit (2026-09-22)

Files audited: `apps/desktop/src/components/pages/SettingsPage.tsx` (1,750 lines), `apps/desktop/src/components/settings/{LabelManager,BackupSection,GoogleCalendarSection,ReminderSection,VaultSection,TodoistSyncSection,TodoistMigrationSection,DocsMigrationSection,TasksMigrationSection}.tsx`, `apps/desktop/src/components/setup/SetupDialog.tsx`; composed primitives read for anchors: `components/ui/{input,button,switch,toggle,toggle-group,select,alert-dialog,label,dialog}.tsx`, `components/shared/{typography,IconButton,PageHeader}.tsx`, `lib/{labelColors,fonts}.ts`, `hooks/useTheme.ts`, `components/layout/Dashboard.tsx:216-231` (scroll shell), token layer `index.css` + `themes.css` (warm only). `TypographyTuner` is DEV-gated in `App.tsx:168` and out of scope per brief.

Screenshots (all in `docs/audit-findings/screenshots/loop1-before/`): baselines `settings-light.png`, `settings-dark.png`, `settings-light-full.png` (1280×5580); new this loop `setup-light.png`, `setup-dark.png` (SetupDialog via `setupComplete:false`), `settings-input-focus-dark.png` (focus ring on Todoist token field), `settings-label-hover-dark.png` (label row hover, trash revealed), `settings-bottom-dark.png` (scrolled to end — page header gone, nav still stuck), `settings-scrolled-light.png` (scrollTop 1500 — page header gone). Contrast numbers below were measured in-browser by compositing the live token values on a canvas (not estimated).

## Scorecard
| Dimension | Score | Evidence |
| --- | --- | --- |
| UI / visual | 2/5 | 24 Title-Case strings against §3.4 "sentence case throughout"; 13 hex + 19 raw Tailwind palette classes (`bg-green-500`, `text-amber-500`) where `--success` exists; 12/20/24px spacing at 39 sites (§3.5); inputs 32px/10px-radius beside Save buttons 28px/8px-radius/12.8px text; two hand-rolled segmented controls while `ToggleGroup` sits unused |
| Interaction | 2/5 | Hover fills on nav links and label rows measure 1.02:1 vs background in both themes (invisible); secret Show/Hide is `tabIndex={-1}`; focused delete button stays at `opacity:0`; page header stops sticking after 759px of scroll; SetupDialog's X and Escape verified dead |
| UX | 2/5 | 19 sections in one 5,559px scroll with one-time migrations, `Seed Existing Data`, and a "coming soon" stub as peers of Appearance; section nav omits 2 sections, lists Obsidian 8th but renders it 17th, has no active state; Vault says "set your vault path above" while the field is 4,000px below; setup blocks on a field nothing reads. No-guilt copy is good throughout ("still open"-style framing, "they'll retry automatically") |
| Accessibility | 3/5 | `htmlFor` on every settings field, `aria-label` on switches/trash/color triggers, `role=alert/status` on async copy, focus lands in the first setup input — but 13 swatch/icon picker buttons and the inline label-rename input are unlabeled, dark input borders measure 1.33:1 (needs 3:1), 12px muted text on dark inputs 4.37:1, 11px `muted-foreground/70` 2.79:1 |
Average: 2.3

## Findings (P1 first, then P2, then P3)

### P1-1 Title Case across labels, buttons, nav and setup — the rubric says sentence case throughout
- Anchor: `SettingsPage.tsx:77,84,94,104,111` (field labels "Todoist API Token", "Anthropic API Key", "Vault Path", "Break Duration (minutes)", "Status on Abandon"); `:1312,1327,1332,1353` (nav "Focus Mode", "Status Colors", "Capture Routes", "Demo Mode"); `:393,833,1080,1103,1120,1132,1150,1683` ("Add Calendar", "Update Route"/"Add Route", "Auth Token", "Test Connection", "Initialize Remote Database", "Sync Now", "Seed Existing Data", "Check for Updates"); `:760,768` ("Doc Note", "Linked Doc"); `GoogleCalendarSection.tsx:70-71` ("Keep Nimble", "Use Calendar"); `SetupDialog.tsx:31,38,44,50,128` (all four labels + "Get Started"); plus `capitalize` CSS on `:757` and `:1383`
- Rubric: §3.4 ("Nimble uses sentence case throughout")
- Screens: light `settings-light-full.png`, dark `settings-dark.png`; setup `setup-light.png` / `setup-dark.png`
- What's wrong: 24 strings are Title Case while their neighbours are sentence case ("Todoist sync", "Import from Todoist", "Docs storage format", "Phone alerts", "Add label", "Sync now", "Back up now", "Reset all settings"). The same page shows "Sync Now" (Turso) and "Sync now" (Todoist) 900px apart. Two buttons rely on `text-transform: capitalize` instead of natural copy.
- Fix: rewrite the 24 strings in sentence case and delete both `capitalize` classes (write the copy as "Light/Dark/System" and "Doc note/Task" directly). CROSS-SURFACE: check `TaskDetail` and dialogs for the same drift.

### P1-2 Status colors hardcoded to the Tailwind palette while `--success` exists
- Anchor: `SettingsPage.tsx:1014,1035` (`bg-green-500` connected dot ×3), `:1035,1053` (`bg-amber-500`, `text-amber-500` pending), `:1110` (`border-amber-500/30 bg-amber-500/5` callout), `:463-473` (`COLOR_OPTIONS` — nine raw `text-*-500`/`bg-*-500` pairs), `:209-216` + `:519-527` (13 hex literals `FEED_COLORS`/`ROUTE_COLORS`)
- Rubric: §3.4 ("Hardcoded colors instead of frame theme vars"), §1.4 (color reserved for semantic meaning)
- Screens: light `settings-light-full.png` (Sync "Connected" dot, Status Colors list), dark `settings-label-hover-dark.png` (green dot at y≈725)
- What's wrong: `themes.css:29` defines `--success` (and the dark variant at `:65`) for exactly this role; the Sync section paints its own green. There is no warning token, so "pending" reaches for `amber-500`, which does not shift between themes. The three category-color pickers use three unrelated palettes (6 hex feeds, 7 hex routes, 10 named Todoist colors in `labelColors.ts:52`), so a "green" calendar, route and label are three different greens.
- Fix: `bg-success` for connected; add `--warning` (light `oklch(0.75 0.15 75)`, dark `oklch(0.8 0.14 80)`) to `themes.css` and use it for pending/needs-initialization; collapse `FEED_COLORS` and `ROUTE_COLORS` onto `LABEL_COLOR_OPTIONS` + `labelColor()` so one swatch palette serves all three pickers.

### P1-3 Keyboard users cannot reveal a secret or see the delete control they just focused
- Anchor: `SettingsPage.tsx:156` (`tabIndex={-1}` on Show/Hide); `LabelManager.tsx:236` (`opacity-0 group-hover:opacity-100`, no `focus-visible:` or `group-focus-within:` rule — measured `opacity: 0` with the button focused and the pointer parked at 5,5)
- Rubric: §1.5 (keyboard-first)
- Screens: light `settings-light.png` (Show at x=923), dark `settings-label-hover-dark.png` (trash visible only because the row is hovered)
- What's wrong: the only way to check a pasted token is a control deliberately removed from the tab order. The label delete button takes focus but stays invisible, so Tab lands on a control with no visible focus ring or icon.
- Fix: drop `tabIndex={-1}` and give Show/Hide the ghost-button focus ring; add `focus-visible:opacity-100 group-focus-within:opacity-100` to the trash `IconButton` (CROSS-SURFACE: same `opacity-0 group-hover:opacity-100` reveal pattern likely lives in task rows — grep it).

### P1-4 Setup blocks on a field nothing in the app reads
- Anchor: `SetupDialog.tsx:36-41` (`ical_feed_url` field — zero consumers anywhere in `apps/desktop/src` or `nimble-core/src`; calendars are stored via `dp.calendar.addFeed`, `SettingsPage.tsx:253`); `:65` (`allFilled` requires all four values before "Get Started" enables)
- Rubric: §3.2 ("Required choices for common actions"), §1.2 (smart defaults reduce decisions to zero)
- Screens: light `setup-light.png`, dark `setup-dark.png` ("Get Started" disabled)
- What's wrong: first run demands a Todoist token, an iCal URL, a vault path and an Anthropic key — four pastes from four browser tabs — and one of the four is discarded. Todoist is being retired (C1–C5), so this gate is also aging out.
- Fix: delete the iCal field; require nothing (the vault path already has a default) and enable "Get started" immediately with the help text "You can add these later in Settings"; keep saving whatever was filled.

### P2-1 Information architecture: 19 flat sections, a nav that disagrees with the page, and ops tooling at the same level as Appearance
- Anchor: `SettingsPage.tsx:1278-1362` (nav: 17 links; missing `#tasks-format` and `#backups`; order Focus→Obsidian→Calendars→Status Colors→…) vs render order `:1578-1745` (Focus→Status Colors→Capture Routes→Labels→Calendars→Sync→Backups→Reminders→Phone alerts→Obsidian); `ariaCurrent` count measured 0; `:1545-1573` (three one-time migrations as permanent sections); `:1134-1151` ("Seed Existing Data"); `:483-506` (Status Colors: "Color customization coming soon" — a section with no control)
- Rubric: §1.2 (decision overhead), §1.4 (restraint / earned attention)
- Screens: light `settings-light-full.png` (5,580px), dark `settings-bottom-dark.png` (nav lists Obsidian 8th; Obsidian is rendered second-to-last)
- What's wrong: measured scroll height 5,559px. The section index exists and sticks (good), but it lists 17 of 19 sections in a different order from the page, never highlights the current section, and gives "Import from Todoist" (one-time) the same weight as "Appearance" (daily). Sync exposes schema-initialization and sync-log seeding — developer verbs — to the user.
- Fix: executable now — generate the nav from the rendered section list (one array drives both), add an IntersectionObserver scroll-spy with `aria-current="true"` + `text-foreground`, move Import/Docs format/Task descriptions format/Seed/Initialize under one collapsed "Maintenance" section at the end, delete the Status Colors stub. Structural regrouping into General / Integrations / Data / Advanced sub-pages: QUEUED FOR MARCO.

### P2-2 "Vault" and "Obsidian" are one feature split 4,000px apart, and the copy points the wrong way
- Anchor: `VaultSection.tsx:40` ("Set your vault path above to index your notes") rendered at `SettingsPage.tsx:1523` (3rd section); the path field is `OBSIDIAN_FIELDS` rendered at `:1633` (17th section, description "Path to your Obsidian vault for Today.md and session logs")
- Rubric: §1.2 (batch information; don't make the user choose where to look)
- Screens: light `settings-light-full.png` (Vault at y≈310, Obsidian at y≈1780 in the 459px-wide render), dark `settings-bottom-dark.png` (Obsidian)
- What's wrong: on a fresh install the Vault section tells you to look "above" for a field that is below the fold by four screens. Two descriptions describe the same path differently.
- Fix: merge into one "Obsidian" section at position 3: path field + save, then index status + "Rescan vault"; delete the `#obsidian` section and its nav link.

### P2-3 Page header stops sticking after ~760px; section nav offset assumes a header height that doesn't exist
- Anchor: `components/layout/Dashboard.tsx:216` (scroller is `flex` row) + `:229` (`main` stretches to the row's 800px cross-axis — measured `main.clientHeight` 800 vs `scrollHeight` 5559); `PageHeader.tsx:27` (`sticky top-0` — containing block is `main`, so header top measured 0 at scrollTop 700, −141 at 900, −741 at 1500); `SettingsPage.tsx:1278` (`sticky top-16` = 64px; the header is 41px tall → 23px dead gap while stuck, and the nav floats 64px from the top once the header is gone)
- Rubric: §3.5 (spacing rhythm / chrome discipline), §1.4 (earned attention — chrome should be predictable)
- Screens: light `settings-scrolled-light.png`, dark `settings-bottom-dark.png` (no "Settings" title; nav at y=64)
- What's wrong: the only page tall enough to expose it is Settings today, but Tasks at 1,440px is 90px from the same cliff. The drag region and page title vanish mid-scroll.
- Fix: CROSS-SURFACE (shell) — make the scroller `flex flex-col` (or `items-start`) so `main` is content-height; then set the settings nav to `top-[41px]`→ better, expose `--page-header-h: 41px` from `PageHeader` and use `top-(--page-header-h)`.

### P2-4 Form controls are three heights, two radii and two type sizes on one row
- Anchor: `SettingsPage.tsx:162-169` (`size="sm"` Save: measured 28px, radius 8px, 12.8px text) next to `:143` (`Input`: 32px, radius 10px, 13px text) and `:152-159` (Show: 17px tall text button); `ui/button.tsx:28` (`sm` uses `text-[0.8rem]` — a raw arbitrary size); `:1376-1393` (Mode: ghost Buttons in `bg-secondary rounded-lg p-1`) vs `:751-763` (route Type: default/outline Buttons in `border p-1`) vs `ui/toggle-group.tsx:45` (the real segmented primitive, `rounded-full bg-muted/60`, unused here); `AlertDialogAction`/`Reset all settings`/`Migrate` use default `h-8` while 30+ sibling buttons use `sm`
- Rubric: §3.4 (raw sizes outside the token scale), cross-surface consistency
- Screens: light `settings-light.png` (Save sits 2px lower than the input top, 2px shorter), dark `settings-input-focus-dark.png`
- What's wrong: the input/button pair that appears nine times on this page never lines up. Three segmented-control looks exist in the codebase; two are hand-rolled here.
- Fix: use `size="default"` (h-8) for every button that sits beside an input; replace both hand-rolled segments with `ToggleGroup`/`ToggleGroupItem`; CROSS-SURFACE: change `button.tsx:28` `text-[0.8rem]` → `text-meta-strong` so `sm` lands on the scale.

### P2-5 Two section systems on one page: heading level, description token and separator rhythm diverge
- Anchor: 13 sections via `SectionHeader` (`SettingsPage.tsx:179-194`: `h2` + `text-body` description, `space-y-4`, followed by `<Separator />` inside `space-y-8`) vs `BackupSection.tsx:91-95`, `ReminderSection.tsx:29-31`, `GoogleCalendarSection.tsx:39-41` (`h3` default from `SectionTitle`, `Meta` 12px description, `border-t border-border pt-6`, no Separator — measured headings list: 13×H2 then H3/H3/H3 then H2 again); field labels: `ui/label.tsx:12` (`font-medium` baked under `text-body-strong` = a font-medium stack) vs bare `<label className="text-body">` (`BackupSection.tsx:125`, `ReminderSection.tsx:40`, `GoogleCalendarSection.tsx:55,57`) vs `text-label text-muted-foreground` (`SettingsPage.tsx:1433,1458`); values in mono (`:1047,1053,1059,1670`) vs sans in the Backups `dl` (`BackupSection.tsx:101`)
- Rubric: §3.4 (`font-medium` stacks; one token per role), §3.5 (spacing rhythm)
- Screens: light `settings-light-full.png` (Backups/Reminders/Phone alerts run at y≈1540–1740 with smaller descriptions), dark `settings-bottom-dark.png`
- What's wrong: peers of "Sync" render one heading level down, with 12px descriptions where every other section uses 13px, and a hairline instead of the Separator. Three field-label styles and two value fonts (mono vs sans) for the same "key: value" role. `tabular-nums` is already global (`index.css:117`), so mono buys nothing.
- Fix: route the three sections through `SectionHeader` + `<Separator />`; one `FieldLabel` primitive (`typography.tsx:93`) everywhere; drop `font-mono` on values (keep it for the device-id `<code>`); CROSS-SURFACE: remove `font-medium` from `ui/label.tsx`.

### P2-6 Spacing off the 8px grid at 39 sites
- Anchor: `p-3` ×13 (e.g. `SettingsPage.tsx:352,729,1067,1110`, `LabelManager.tsx:119`, `*MigrationSection.tsx:81/86/94`), `space-y-3` ×10, `gap-3` ×6, `space-y-5` ×6 (`:1507,1583,1638`, three migration sections), `pt-6` ×3 + `p-6` ×1 (`BackupSection.tsx:91`, `ReminderSection.tsx:29`, `GoogleCalendarSection.tsx:39`, `:1276`)
- Rubric: §3.5 (4 / 8 / 16 / 32 / 64)
- Screens: light `settings-light-full.png`, dark `settings-dark.png`
- What's wrong: 12px, 20px and 24px steps dominate the page's internal rhythm; the section gap is 32px (on grid) but everything inside it isn't.
- Fix: `p-3`→`p-4`, `space-y-3`/`gap-3`→`space-y-4`/`gap-4` (or `-2` for tight rows), `space-y-5`→`space-y-4`, `pt-6`→`pt-8`, page `p-6`→`p-8`.

### P2-7 Accent picker shows six saturated dots for themes that are nearly neutral
- Anchor: `SettingsPage.tsx:198-205` (`swatch` literals — warm `oklch(0.65 0.15 75)`) vs `themes.css:16` (warm `--primary: oklch(0.205 0.01 60)`, chroma 0.01) ; `:1401-1419` (`title` only, no `aria-label`, no `role="radiogroup"`; `text-white` check hardcoded; `scale-110` selected)
- Rubric: §1.4 (color reserved for semantic meaning; warm restrained palette), better-colors "measure the rendered pair / represent the real token"
- Screens: light `settings-light.png` (amber dot for a theme with no amber), dark `settings-dark.png`
- What's wrong: the picker promises an amber, pink, green UI; the warm theme deliberately paints almost nothing in colour. The control that configures the token system does not read from it.
- Fix: render each swatch from its theme's real tokens (a 32px chip with `--background` fill, `--primary` dot, `--border` ring, computed by temporarily reading the `.theme-*` class) with `role="radiogroup"`/`aria-checked` and the label text beside the row; drop `scale-110` (motion on selection, not press).

### P2-8 Font selects display the raw value ("geist") instead of the label ("Geist")
- Anchor: `SettingsPage.tsx:1441` and `:1466` (`<SelectValue />` with no children/`items` — base-ui renders the value key; measured trigger text "geist")
- Rubric: §3.4 (case), consistency (the dropdown list shows "Geist")
- Screens: light `settings-light.png`, dark `settings-dark.png` (both triggers read "geist")
- What's wrong: the one place the app shows off its typography shows a lowercase config key.
- Fix: `<SelectValue>{FONT_OPTIONS.find((f) => f.value === headingFont)?.label}</SelectValue>` (same for body), or pass `items={Object.fromEntries(FONT_OPTIONS.map(f => [f.value, f.label]))}` to `Select`.

### P2-9 Hover states are invisible: `accent/20` on a warm theme where accent ≈ background
- Anchor: `SettingsPage.tsx:1281-1358` (nav links `hover:bg-accent/20` — measured rendered delta 2–3 RGB units, 1.024:1 light / 1.018:1 dark), `LabelManager.tsx:193` (`hover:bg-accent/10` — 1.009:1 / 1.010:1), `SettingsPage.tsx:770` (dropdown trigger `hover:bg-accent/10`), `components/shared/IconButton.tsx:33` (`hover:bg-accent/20`)
- Rubric: §1.6 (every interaction produces immediate visual response), better-colors "use a token only in its role"
- Screens: dark `settings-label-hover-dark.png` (row under the pointer is indistinguishable from its siblings), light `settings-light.png`
- What's wrong: `--accent` is `oklch(0.96 0.005 75)` on a `0.995` background (light) and `0.22` on `0.14` (dark); at 10–20% alpha the fill is below one JND. Buttons already use `hover:bg-muted` at full opacity (`button.tsx:19`), so the page has one visible hover language and one invisible one.
- Fix: `hover:bg-muted` (no alpha) on nav links, label rows, the dropdown trigger and `IconButton`. CROSS-SURFACE: grep `hover:bg-accent/` app-wide (PageHeader back button `PageHeader.tsx:36` uses it too).

### P2-10 Dark-theme input borders and small muted text fall under contrast thresholds (measured)
- Anchor: `themes.css:63` (`--input: oklch(1 0 0 / 12%)`) rendered through `ui/input.tsx:12` `dark:bg-input/30` + `border-input` — border vs background 1.33:1 dark, 1.27:1 light (UI component threshold 3:1); `SettingsPage.tsx:155` (12px `text-muted-foreground` "Show" on the dark input bg: 4.37:1, needs 4.5); `DocsMigrationSection.tsx:97`, `TasksMigrationSection.tsx:102` (11px `text-muted-foreground/70`: 3.25:1 light, 2.79:1 dark); `text-meta text-destructive` errors on dark background 4.18:1 (`SettingsPage.tsx:173,390,1157`)
- Rubric: accessibility dimension; §1.4 ("earned attention" is dimmer, not illegible)
- Screens: dark `settings-dark.png` (inputs read as flat bars), light `settings-light.png`
- What's wrong: in dark mode the field boundary is a 12%-white hairline at 30% alpha — the token was tuned for the fill, then reused for the border. Opacity hacks on already-muted text push 11px copy under 3:1.
- Fix: dark `--input` → `oklch(1 0 0 / 22%)` for the border (keep `/30` fill) or add `dark:border-foreground/15` in `input.tsx` (CROSS-SURFACE); delete the `/70` modifiers (plain `text-muted-foreground` already passes); report the destructive pair and leave the colour alone.

### P2-11 Destructive actions: five confirm modals, two heavy actions with none, and no undo anywhere
- Anchor: confirms — `SettingsPage.tsx:311-340` (remove feed), `:689-717` (delete route), `:1717-1742` (reset), `LabelManager.tsx:231-260` (delete label), three migrations; no confirm — `GoogleCalendarSection.tsx:49` ("Disconnect", ghost button, one click), `SettingsPage.tsx:451-456` (Demo mode `Switch` restarts the app on toggle, `:427-434`); no undo path in any handler
- Rubric: §3.2 (modal interruptions for trivial confirmations; one-step where one-step works), §1.6 (optimistic + instant feedback)
- Screens: light `settings-light-full.png` (Remove/Delete/Reset), dark `settings-label-hover-dark.png` (Remove ×2, trash)
- What's wrong: deleting a label — the most frequent of these — costs a modal, while flipping a switch relaunches the app with no warning. Linear's pattern for the frequent case is remove-now + toast Undo.
- Fix: label/route delete → optimistic removal + `taskToast`-style "Undo" (restore needs a soft-delete or recreate-with-id → NEEDS RUST → queue for Marco); wrap Disconnect in the same `AlertDialog` as Remove feed; confirm the demo toggle since it restarts.

### P2-12 Label manager will fight C4 grouping, and its inline rename is invisible and unlabeled
- Anchor: `LabelManager.tsx:102-115` (flat `space-y-0.5` list, no group affordance), `:119-154` (add form: name + colour only), `:220-229` (bare `<input>` — no `aria-label`, no border, no focus ring, no hover affordance; Enter/Escape handled), `lib/labelColors.ts:52-63` (ten free-choice Todoist colours)
- Rubric: §3.6 (inline editing where Linear would have it — present but undiscoverable), §1.2 (chunking), §1.5 (keyboard/AT)
- Screens: light `settings-light-full.png` (Labels at y≈1035), dark `settings-label-hover-dark.png`
- What's wrong: rows look read-only; a click reveals a text field with nothing to say it is one. Nothing in the row, the form or the colour model carries an ENERGY/TIME/TYPE/CREATIVE group, so C4 will need group headers, a group picker, move-between-groups and (likely) a per-group colour default rather than a free palette.
- Fix: now — `aria-label={\`Rename ${label.name}\`}` and `focus-visible:ring-3 focus-visible:ring-ring/50 rounded-md px-1` on the input, a hover underline on the text; structure — render `labels` grouped under `Label` (11px) group headers with a group `Select` in the add form, palette defaulting per group → QUEUED FOR MARCO (align with the C4 spec before building).

### P2-13 Type scale drift (reported once per brief)
- Anchor: `docs/typography-system.md:9-18` (10 tokens: caption 10 / label 11 / meta 12 / body 14 / body-strong 14 / heading-sm 15 / heading 16 / display 20 / display-xl 26 / timer 48; `SectionTitle` = `text-heading-sm`) vs `apps/desktop/src/index.css:38-53` (8 tokens: label 11 / meta 12 / meta-strong 12 / body 13 / body-strong 13 / title 15 / display 20 / timer 48; `SectionTitle` = `text-title`, `typography.tsx:106`)
- Rubric: §3.4
- Screens: n/a
- What's wrong: the doc and the CSS name different tokens and different body sizes; `text-heading`, `text-heading-sm`, `text-caption`, `text-display-xl` do not exist in the build.
- Fix: rewrite `docs/typography-system.md` to the live 8-token scale (the CSS comment at `index.css:31-37` already cites the Linear-pass decision).

### P2-14 SetupDialog shows a close X that does nothing, and Escape is a no-op
- Anchor: `SetupDialog.tsx:86-89` (`<Dialog open={open}>` with no `onOpenChange`; `DialogContent` defaults `showCloseButton` to true, `ui/dialog.tsx:45,62-76`) — verified: click on X and Escape both leave the dialog open, `setupComplete` stays `false`
- Rubric: §3.6 (Escape doesn't return to previous state), §1.4 (restraint — no dead chrome)
- Screens: light `setup-light.png` (X at 873,163), dark `setup-dark.png`
- What's wrong: the first control a new user sees does nothing. If setup is mandatory the X should not exist; if it is skippable (see P1-4) the X should skip.
- Fix: `showCloseButton={false}` today; once P1-4 makes setup skippable, wire `onOpenChange={() => onComplete()}` instead.

### P2-15 Unlabeled icon-only pickers
- Anchor: `SettingsPage.tsx:376-386` (6 feed colour buttons, no `aria-label`), `:795-805` (7 route colour buttons), `:815-826` (4 icon buttons, no label), `:1401-1419` (6 accent swatches, `title` only), `:1278` (`<nav>` without `aria-label` — the shell also has a `<nav>`)
- Rubric: §1.5 (keyboard-first / assistive tech), accessibility dimension
- Screens: light `settings-light.png` (accent row), dark `settings-dark.png`
- What's wrong: 23 buttons announce as "button" with no name; two `<nav>` landmarks with no distinguishing label. `LabelManager.tsx:138,213` already does this right (`aria-label={\`Set color ${c}\`}`).
- Fix: `aria-label` on every swatch/icon button, `role="radiogroup"` + `aria-checked` on each picker, `aria-label="Settings sections"` on the nav.

### P2-16 Raw Rust errors surfaced verbatim, while sibling sections map codes to human copy
- Anchor: `SettingsPage.tsx:623,635` (`toast.error(\`Failed: ${e}\`)`), `:260,271,931,950,968,983,1145` (`setError(String(e))` / `\`Connection failed: ${e}\``), `LabelManager.tsx:38,54,66,76,86` vs `BackupSection.tsx:9-29` and `GoogleCalendarSection.tsx:36` (code → sentence maps)
- Rubric: §1.1 (neutral framing, the app doesn't shout), cross-surface consistency
- Screens: n/a (error state)
- What's wrong: half the page speaks to the user ("Choose a private repository. Nothing was uploaded."); the other half prints `Failed: Error: invoke ...`.
- Fix: one `settingsMessage(code)` helper beside `backupMessage`, with a neutral default ("That didn't save. Try again.") and the raw string in a collapsed `<details>`.

### P3-1 `transition-all` and selection-scale on pickers
- Anchor: `SettingsPage.tsx:379,799,819,1405`; `LabelManager.tsx:133,196,208` (7 × `transition-all`, no duration token); `scale-110` as the selected state at `:381,800,1407`, `LabelManager.tsx:134,209`
- Rubric: §1.6 (ease-out, restrained motion); craft: make-interfaces-feel-better "Never use `transition: all`" + "Motion restraint" (scale is for press at 0.96, not a persistent selected state)
- Screens: light `settings-light.png` (selected warm swatch is 35px vs 32px siblings — measured), dark `settings-dark.png`
- What's wrong: every colour-swatch and icon button transitions every property with the browser default 0ms→inherit timing, bypassing `--transition-fast`; the selected swatch is physically bigger, which misaligns the row.
- Fix: `transition-[border-color,background-color] duration-(--transition-fast)`; selected = `ring-2 ring-foreground ring-offset-2 ring-offset-background`, no scale.

### P3-2 Three-period "..." in 12 labels, single-character "…" in 5
- Anchor: `SettingsPage.tsx:168,393,432,833,1095,1103,1120,1132,1683,1721`; `LabelManager.tsx:144`; `SetupDialog.tsx:128` vs `VaultSection.tsx:53`, `TodoistSyncSection.tsx:82`, three migration sections (`Checking…`, `Converting…`)
- Rubric: §3.4 (type discipline); craft: better-typography "the single ellipsis character, not three periods"
- Screens: light `settings-light-full.png`
- What's wrong: two spellings of the same busy state on one page.
- Fix: replace the 12 with `…`.

### P3-3 Inner controls have a larger radius than the box around them
- Anchor: `SettingsPage.tsx:352,729,1067` and `LabelManager.tsx:119` (`rounded-md` = 8px boxes, `p-3` = 12px padding) wrapping `Input`/`Button` at `rounded-lg` = 10px (`input.tsx:12`, `button.tsx:9`); list rows at `:299,663` (`rounded-md`) with the same inner controls
- Rubric: §3.4; craft: make-interfaces-feel-better "Concentric border radius" (outer = inner + padding)
- Screens: light `settings-light-full.png` (Turso config box, Add-form boxes), dark `settings-label-hover-dark.png` (feed rows)
- What's wrong: 29 `rounded-md` vs 9 `rounded-lg` on the page; every form box's corners are tighter than the inputs inside them, which reads as pinched.
- Fix: form boxes `rounded-2xl p-3` (18px ≈ 10 + 8; or `rounded-[calc(var(--radius)+12px)]`), list rows `rounded-lg` with `px-3 py-2` and no inner-radius children.

## Cross-surface notes
- `components/layout/Dashboard.tsx:216` scroller is a flex row; `main` is stretched to viewport height, so every page's `PageHeader` stops sticking once content exceeds ~1,560px (Settings today, Tasks soon). Fix belongs to the shell.
- `ui/button.tsx:28` (`sm` → `text-[0.8rem]`) and `ui/toggle.tsx:16` are the only raw font sizes in the primitives; they leak onto every surface that uses `size="sm"`.
- `ui/label.tsx:12` bakes `font-medium` under whatever type token callers add — every `<Label className="text-body-strong">` in the app is a font-medium stack (§3.4).
- `hover:bg-accent/10|20` is a no-op in the warm theme (accent ≈ background); `IconButton.tsx:33` and `PageHeader.tsx:36` carry it, so Tasks/Docs/Inbox icon buttons likely have the same invisible hover.
- Dark `--input` (`themes.css:63`, 12% white) fails 3:1 as a border everywhere `border-input` is used, not just here.
- `ui/toggle-group.tsx` is a finished segmented-control primitive; Settings hand-rolls two others. Worth checking whether Tasks filters/Goals views do the same.
- Three unrelated category-colour palettes (feeds, routes, labels) — the Tasks label chips and calendar rail will show three different "greens".
- No-guilt language is clean on this surface; nothing to carry over.
