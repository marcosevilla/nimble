# Agentation pass 3 — shell polish, task detail consolidation, Normal priority icon

**Source:** Marco's Agentation notes 2026-09-24 on `?page=tasks` (10 notes) against main `39430f4`. Decisions (Marco, 2026-09-24): wordmark = C3 Stamp mark + "Nimble" in the app's heading font; sync toast X hides it until the next problem (or relaunch while still unhealthy); same QA-first pipeline as chunk 3 (`docs/superpowers/plans/2026-09-24-loop2-chunk3.md` → Pipeline + Standing checks apply unchanged: frozen builds via `tools/qa-frozen.sh`, e2e in `apps/desktop/e2e/`, clipping / focus ring / axe baseline, before/after light+dark screenshots to `~/Developer/second-brain/outputs/qa/<task>/`, max 5 iterations, nothing merges until Marco approves).

**Motion rule (all animations):** use the existing motion tokens (loop 1 Stage C2: no `duration-N` literals, no `transition-all`), 150–250 ms, ease-out; honour `prefers-reduced-motion` (no movement, instant or opacity-only). Tests assert the reduced-motion path is instant and that the animated path ends in the same final layout.

Two lanes, disjoint files: **A** shell (NavSidebar/NavTrees/RightSidebar/SyncHealthBanner/Dashboard), **B** task detail + priority icon (TaskDetailPage/MetadataChips/ReminderPicker/PriorityBars/RowMarks).

---

## A — Shell polish (`loop3/a-shell`)

1. **Wordmark (#1).** The sidebar header (`components/layout/NavSidebar.tsx`, the `.mb-1` row holding the collapse button) shows the C3 Stamp mark (~18px, from the existing icon source — `favicon.svg`/`source.svg` flat vector, inlined or imported as an asset, currentColor-aware if possible so it reads in dark mode) + "Nimble" in the heading font, semibold, body-strong size. When the sidebar is snapped to icons, only the mark shows. It doesn't push the collapse button or the nav items down (header height unchanged), text doesn't clip at the min sidebar width (160px), and it is not a focus stop unless it does something (decorative → `aria-hidden` mark; the text can be plain).
2. **Condensed nav trees (#2).** Tasks' project tree and Docs' folder/vault tree rows get tighter vertical rhythm (target: row pitch ~28px instead of today's; keep ≥24px hit height and the existing focus ring fully visible, no clipped text or counts). Top-level page items (Today, Tasks, Inbox, Docs, Goals) are unchanged unless they share the same spacing class — say which in the report.
3. **Tree expand/collapse motion (#3).** Chevron rotates smoothly and the child list animates height/opacity open and closed (nav page trees + nested project trees). Keyboard (→/←, Enter) and click paths both animate; focus stays on the toggled item; no layout jump at the end; reduced motion = instant.
4. **Sync toast (#4).** `SyncHealthBanner` stops being a top banner (the page no longer shifts down when it appears). The same message + "Sync now" (+ "Open settings" when it deep-links today) render as a persistent bottom-right toast-style surface (reuse sonner with `duration: Infinity` and a stable id, or a small fixed component using the toast tokens — builder's call, keep one toast per problem, never stacked duplicates). It sits above the `?` help button (no overlap at 1440×900 and 1024×700), doesn't cover the right rail's content controls, and has an X (accessible name "Dismiss") that hides it for this app session; it reappears only if sync health changes to a new problem, or on relaunch while still unhealthy. Keyboard reachable, focus ring, `role=status`/polite live region so it's announced once, no clipping, no new axe violations.
5. **Right rail tab motion (#5).** Switching Calendar/Habits/Activity/Focus queue tabs animates: the active indicator slides between tabs and the panel content cross-fades (no jagged jump, no layout shift of the tab bar). Keyboard arrow navigation between tabs still works; reduced motion = instant.

## B — Task detail consolidation + Normal priority icon (`loop3/b-task-detail`)

1. **Reminder as a pill (#6).** The task detail page's separate reminder row (`section` "Task reminder") is removed; reminder becomes a chip in the metadata chip row next to priority/labels/project/due, using the same chip anatomy and the existing `ReminderPicker` in a popover. Chip text shows the current reminder (e.g. "Remind 15 min before") or a quiet "Reminder" when none; all current reminder capabilities (set/clear offset, whatever the row offered) remain reachable. If the reminder needs a due time and there is none, the chip explains that in its picker (same rule as today). This closes NEXT.md item 5's "fold Reminder into the chip row".
2. **Focus actions into the task menu (#7) + "…" icon (#8).** The header row's Focus controls (the "Focus" group next to the title) move into the task-actions menu as menu items (same actions, same labels/shortcuts shown); the menu trigger changes from the gear (`Settings` icon) to a horizontal "…" (`MoreHorizontal`, the icon other ⋯ menus use) with accessible name "Task actions". Nothing that was reachable before becomes unreachable; ⇧F / focus shortcuts unchanged.
3. **Back link (#9).** The breadcrumb/back control at the top of the detail page (`ChevronLeft` + text, TaskDetailPage ~377) is one button/link: chevron and text both navigate back, one focus stop, visible focus ring; text one step larger (the next type-scale token up) and the chevron sized to match; hit area ≥ 28px tall — prominent but not a heading.
4. **Normal priority icon (#10).** Priority "Normal" renders an empty signal icon (same 3-bar glyph, all bars in the muted/empty tone) everywhere `PriorityBars` shows — task rows, the detail chip, pickers/menus (the menu's "Normal" item gets the icon too, so every option has one). Rows therefore always have a priority mark: Normal rows get the clickable priority mark (T1 contract "Priority: normal"), `p` still works. Row layout doesn't shift (the slot already exists or the title x stays identical to rows with bars). Update T1/T2 specs where they assumed Normal has no mark (note it in the test with "Marco 2026-09-24: Normal gets an empty icon").
