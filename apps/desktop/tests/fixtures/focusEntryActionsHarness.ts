// Bundled by tests/focusTaskEntry.test.mjs (Vite SSR) so the shortcut's
// Focus now action, the focus store and the provider accessor share one
// module instance, like the running app.
export { focusNowForTask } from '../../src/components/focus/focusEntryActions'
export { resetFocusCache } from '../../src/stores/focusStore'
export { setDataProvider } from '../../src/services/provider-context'
