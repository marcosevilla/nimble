// Bundled by tests/focusStore.test.mjs (Vite SSR) so the store and the
// provider accessor share one module instance, like the running app.
export { useFocusCache, refreshFocus, sendFocusAction, connectFocusCache, resetFocusCache } from '../../src/stores/focusStore'
export { setDataProvider } from '../../src/services/provider-context'
export { notifyFocusChanged, FocusRequestError } from '../../src/services/focus-events'
